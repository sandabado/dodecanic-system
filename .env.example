# Public browser credentials from Supabase project settings → API.
# These values identify the project but do not grant service-role access.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Never place a Supabase secret/service-role key in a NEXT_PUBLIC_ variable.
